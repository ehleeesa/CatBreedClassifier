# CatBreedClassifier
Cat breed recognition system built with PyTorch and a transfer-learning ResNet-18 model, trained on the Oxford-IIIT Pet Dataset. Features complete preprocessing, training/validation pipeline, performance visualization, and a Gradio interface for interactive predictions.
